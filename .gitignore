# Secrets — never commit API keys
.env
.env.*
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Data & build artifacts (large / reproducible — keep the repo lean)
data/raw/
data/filings/
data/chunks.jsonl
*.faiss
*.index
indexes/
.cache/
# data/manifest.json is intentionally tracked: small, records provenance
# (accession/URL + corpus_hash) for reproducibility.

# Eval run outputs (history can be noisy; keep baseline.json tracked explicitly)
runs/
metrics.json
metrics_ablation.json

# OS / editor
.DS_Store
.idea/
.vscode/
